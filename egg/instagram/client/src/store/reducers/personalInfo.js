const defaultValue = {
  topic: {
    counts: 0,
    topicList: []
  },
  fansCounts: 0,
  followCounts: 0
}

const personalInfo = (state = defaultValue, action) => {
  switch(action.type) {
    default:
      return state
  }
}

export default personalInfo;