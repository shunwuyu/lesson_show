const initialState = {
  newUser: null,
  error: null,
  saveSuccess: false
}
export const users = (state = initialState, action = {}) => {
  switch(action.type) {
    default:
      return state
  }
}