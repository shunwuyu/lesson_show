<block wx:for="{{products}}" wx:key="_id">
  <van-card 
    price="{{item.view}}"
    desc="{{item.color}}"
    title="{{item.title}}"
    thumb="{{item.image}}"
    data-id="{{item._id}}"
    bindtap="click"
    />
</block>